const Posts = require("../models/Posts");
const Details = require("../models/Details");

module.exports = {
    async PostsCreate (req, res)  {
        try {
            
        } catch (err) {
            console.error(err)
        }
    }
}